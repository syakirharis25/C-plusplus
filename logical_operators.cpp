#include<iostream>
using namespace std;
int main()
{
    bool x,y;
    x=true; y=false;
    cout<<"Logical AND Operator: "<<(x&&y)<<endl;
    cout<<"Logical OR Operator: "<<(x||y)<<endl;
    cout<<"Logical NOT Operator: "<<(!x);
    return 0;
}
